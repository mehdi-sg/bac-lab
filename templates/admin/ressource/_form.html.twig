{{ form_start(form, { attr: { class: 'needs-validation' } }) }}

<div class="row g-3">
    <div class="col-12">
        {{ form_row(form.titre, { label: 'Titre' }) }}
    </div>

    <div class="col-12">
        {{ form_row(form.description, { label: 'Description' }) }}
    </div>

    <div class="col-md-6">
        {{ form_row(form.auteur, { label: 'Auteur' }) }}
    </div>

    <div class="col-md-6">
        {{ form_row(form.typeFichier, { label: 'Type de fichier' }) }}
    </div>

    <div class="col-md-6">
        {{ form_row(form.categorie, { label: 'Catégorie' }) }}
    </div>

    <div class="col-md-6">
        {{ form_row(form.tags, { label: 'Tags' }) }}
    </div>

    <div class="col-12">
        {{ form_row(form.imageCouverture, { label: 'Image de couverture (URL)' }) }}
    </div>

    <div class="col-12">
        <div class="form-check">
            {{ form_widget(form.estActive) }}
            {{ form_label(form.estActive, 'Ressource active') }}
        </div>
    </div>
</div>

<div class="mt-4 d-flex gap-2">
    <button class="btn btn-primary px-4">
        <i class="fas fa-save me-2"></i>{{ button_label|default('Enregistrer') }}
    </button>
    <a class="btn btn-outline-secondary" href="{{ path('app_admin_ressource_index') }}">
        <i class="fas fa-arrow-left me-2"></i>Retour
    </a>
</div>

{{ form_end(form) }}
