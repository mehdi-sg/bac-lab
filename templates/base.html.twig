<!doctype html>
<html class="no-js" lang="fr">
<head>
    <meta charset="utf-8">
    <meta http-equiv="x-ua-compatible" content="ie=edge">
    <title>{% block title %}BacLab | Plateforme de Révision & Orientation{% endblock %}</title>
    <meta name="description" content="Plateforme éducative pour les bacheliers tunisiens : révisions, ressources et calcul de score.">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <link rel="shortcut icon" type="image/x-icon" href="{{ asset('front/img/favicon.ico') }}">

    {% block css %}
        {# Essential CSS only - Bootstrap for grid system and FontAwesome for icons #}
        <link rel="stylesheet" href="{{ asset('front/css/bootstrap.min.css') }}">
        <link rel="stylesheet" href="{{ asset('front/css/fontawesome-all.min.css') }}">
        
        {# CDN Fallback for FontAwesome in case local files don't load #}
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" integrity="sha512-1ycn6IcaQQ40/MKBW2W4Rhis/DbILU74C1vSrLJxCq57o941Ym01SwNsOMqvEBFlcgUa6xLiPY/NS5R+E6ztJQ==" crossorigin="anonymous" referrerpolicy="no-referrer" />
        
        {# Modern Design System - Load in correct order #}
        <link rel="stylesheet" href="{{ asset('front/css/navbar-new.css') }}">
        <link rel="stylesheet" href="{{ asset('front/css/footer-modern.css') }}">
        
        {# Fiche-specific CSS (scoped to .fiche-page) #}
        <link rel="stylesheet" href="{{ asset('front/css/fiche-complete.css') }}">
        <link rel="stylesheet" href="{{ asset('front/css/fiche-editor.css') }}">
        <link rel="stylesheet" href="{{ asset('front/css/fiche-show.css') }}">
        
        {# Pages design CSS - conditionally loaded for non-special pages #}
        {% set specialPages = ['app_home', 'app_login', 'app_register', 'app_profile', 'app_profile_edit'] %}
        {% if app.request.attributes.get('_route') not in specialPages %}
            <link rel="stylesheet" href="{{ asset('front/css/pages-design.css') }}">
        {% endif %}
    {% endblock %}

    {% block inline_css %}
        <style>
            /* Clean inline styles */
        </style>
    {% endblock %}
</head>

<body>
<div id="preloader-active">
    <div class="preloader d-flex align-items-center justify-content-center">
        <div class="preloader-inner position-relative">
            <div class="preloader-circle"></div>
            <div class="preloader-img pere-text">
                <img src="{{ asset('front/img/logo/loder.png') }}" alt="Chargement...">
            </div>
        </div>
    </div>
</div>

<header>
    <div class="header-area header-transparent">
        <!-- Scroll Progress Indicator -->
        <div class="navbar-scroll-progress" id="navbarScrollProgress"></div>
        
        <div class="main-header">
            <div class="header-bottom header-sticky">
                <div class="container-fluid">
                    <div class="row align-items-center">

                        <div class="col-xl-2 col-lg-2">
                            <div class="logo">
                                <a href="{{ path('app_home') }}" class="logo-link">
                                    <img src="{{ asset('front/img/logo/logo.png') }}" alt="BacLab Logo" class="logo-img">
                                    <div class="logo-glow"></div>
                                </a>
                            </div>
                        </div>

                        <div class="col-xl-10 col-lg-10">
                            <div class="menu-wrapper d-flex align-items-center justify-content-end">

                                <div class="main-menu d-none d-lg-block">
                                    <nav>
                                        <ul id="navigation">
                                            <li><a href="{{ path('app_home') }}">Accueil</a></li>

                                            <li>
                                                <a href="#">Revision</a>
                                                <ul class="submenu">
                                                    <li><a href="{{ path('app_revision_filieres') }}"><i class="fas fa-file-pdf"></i>Cours & PDF</a></li>
                                                    <li><a href="{{ path('app_quiz') }}"><i class="fas fa-question-circle"></i>Quiz & Tests</a></li>
                                                    <li><a href="{{ path('fiche_index') }}"><i class="fas fa-file-alt"></i>Fiches & co-édition</a></li>
                                                    <li><a href="{{ path('app_bibliotheque_index') }}"><i class="fas fa-book"></i>Bibliothèque</a></li>
                                                </ul>
                                            </li>

                                            <li>
                                                <a href="#">Orientation</a>
                                                <ul class="submenu">
                                                    <li>
                                                        <a href="{{ path('score_calcul') }}" {% if not is_granted('ROLE_USER') %}title="Connexion requise"{% endif %}>
                                                            <i class="fas fa-calculator"></i>Calcul du Score BAC
                                                            {% if not is_granted('ROLE_USER') %}
                                                                <i class="fas fa-lock ml-1" style="font-size: 0.7em; opacity: 0.7;"></i>
                                                            {% endif %}
                                                        </a>
                                                    </li>
                                                    <li>
                                                        <a href="{{ path('orientation_recommendations') }}" {% if not is_granted('ROLE_USER') %}title="Connexion requise"{% endif %}>
                                                            <i class="fas fa-compass"></i>Recommandations
                                                            {% if not is_granted('ROLE_USER') %}
                                                                <i class="fas fa-lock ml-1" style="font-size: 0.7em; opacity: 0.7;"></i>
                                                            {% endif %}
                                                        </a>
                                                    </li>
                                                    <li><a href="#"><i class="fas fa-graduation-cap"></i>Guide des Filieres</a></li>
                                                </ul>
                                            </li>

                                            <li>
                                                <a href="#">Communaute</a>
                                                <ul class="submenu">
                                                    <li><a href="{{ path('groupe_index') }}"><i class="fas fa-users"></i>Groupes de revision</a></li>
                                                    <li><a href="{{ path('chat_index') }}"><i class="fas fa-comments"></i>Chat en direct</a></li>
                                                </ul>
                                            </li>

                                            {% if is_granted('ROLE_USER') %}
                                                <li>
                                                    <a href="{{ path('notifications_index') }}" class="notification-bell" title="Notifications">
                                                        <i class="fas fa-bell"></i>
                                                        <span id="notification-badge" class="badge-pulse">0</span>
                                                    </a>
                                                </li>
                                                <li>
                                                    <a href="{{ path('app_profile') }}" class="notification-bell" title="Mon Profil" style="position: relative !important; overflow: hidden !important;">
                                                        {% if app.user.profil and app.user.profil.profilePicture %}
                                                            <img src="{{ asset('front/assets/img/profile_pictures/' ~ app.user.profil.profilePicture) }}" alt="Photo de profil" style="width: 20px !important; height: 20px !important; border-radius: 50% !important; object-fit: cover !important; object-position: center !important; position: absolute !important; top: 50% !important; left: 50% !important; transform: translate(-50%, -50%) !important; max-width: 20px !important; max-height: 20px !important; min-width: 20px !important; min-height: 20px !important; border: 1px solid rgba(255, 255, 255, 0.3) !important; box-sizing: border-box !important;">
                                                        {% elseif app.user.profil %}
                                                            <div style="width: 20px !important; height: 20px !important; border-radius: 50% !important; background: linear-gradient(135deg, #00d4ff, #b794f6) !important; display: flex !important; align-items: center !important; justify-content: center !important; font-size: 8px !important; font-weight: 700 !important; color: white !important; position: absolute !important; top: 50% !important; left: 50% !important; transform: translate(-50%, -50%) !important; max-width: 20px !important; max-height: 20px !important; min-width: 20px !important; min-height: 20px !important;">{{ app.user.profil.prenom|first|upper }}{{ app.user.profil.nom|first|upper }}</div>
                                                        {% else %}
                                                            <i class="fas fa-user"></i>
                                                        {% endif %}
                                                    </a>
                                                </li>
                                                <li><a href="{{ path('app_logout') }}" class="btn btn3"><i class="fas fa-sign-out-alt"></i>Deconnexion</a></li>
                                            {% else %}
                                                <li><a href="{{ path('app_register') }}" class="btn"><i class="fas fa-user-plus"></i>S'inscrire</a></li>
                                                <li><a href="{{ path('app_login') }}" class="btn btn3"><i class="fas fa-sign-in-alt"></i>Connexion</a></li>
                                            {% endif %}
                                        </ul>
                                    </nav>
                                </div>

                            </div>
                        </div>

                        <div class="col-12">
                            <div class="mobile_menu d-block d-lg-none"></div>
                        </div>

                    </div>
                </div>
            </div>
        </div>
    </div>
</header>

<main>
    {% block body %}{% endblock %}
</main>

<footer style="background: transparent; color: white; padding: 60px 0 30px; border-top: 1px solid rgba(255, 255, 255, 0.1); position: relative;">
    <!-- Simple Background Effect -->
    <div style="position: absolute; top: 0; left: 0; right: 0; bottom: 0; background: linear-gradient(135deg, rgba(26, 26, 46, 0.3) 0%, rgba(10, 10, 15, 0.8) 100%); pointer-events: none;"></div>
    
    <div class="container" style="position: relative; z-index: 2;">
        <!-- Main Footer Content -->
        <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(250px, 1fr)); gap: 40px; margin-bottom: 40px;">
            
            <!-- Brand Section -->
            <div>
                <div style="margin-bottom: 20px;">
                    <img src="{{ asset('front/img/logo/logo.png') }}" alt="BacLab" style="height: 40px;">
                </div>
                <p style="color: rgba(255, 255, 255, 0.8); line-height: 1.6; margin-bottom: 25px; font-size: 14px;">
                    La plateforme #1 en Tunisie pour réussir votre BAC. 
                    Rejoignez 10,000+ étudiants qui nous font confiance.
                </p>
                <div style="display: flex; gap: 20px;">
                    <div style="text-align: center;">
                        <div style="font-size: 1.5rem; font-weight: 800; color: var(--neon-blue);">10K+</div>
                        <div style="font-size: 12px; color: rgba(255, 255, 255, 0.6);">Étudiants</div>
                    </div>
                    <div style="text-align: center;">
                        <div style="font-size: 1.5rem; font-weight: 800; color: var(--neon-purple);">95%</div>
                        <div style="font-size: 12px; color: rgba(255, 255, 255, 0.6);">Réussite</div>
                    </div>
                    <div style="text-align: center;">
                        <div style="font-size: 1.5rem; font-weight: 800; color: var(--neon-pink);">500+</div>
                        <div style="font-size: 12px; color: rgba(255, 255, 255, 0.6);">Ressources</div>
                    </div>
                </div>
            </div>
            
            <!-- Quick Links -->
            <div>
                <h4 style="color: white; font-size: 16px; font-weight: 700; margin-bottom: 20px; display: flex; align-items: center; gap: 8px;">
                    <i class="fas fa-book" style="color: var(--neon-blue);"></i>
                    Révision
                </h4>
                <ul style="list-style: none; padding: 0; margin: 0;">
                    <li style="margin-bottom: 12px;">
                        <a href="{{ path('app_bibliotheque_index') }}" style="color: rgba(255, 255, 255, 0.7); text-decoration: none; font-size: 14px; transition: all 0.3s ease; display: flex; align-items: center; gap: 8px;">
                            <i class="fas fa-chevron-right" style="font-size: 10px; color: var(--neon-blue);"></i>
                            Bibliothèque
                        </a>
                    </li>
                    <li style="margin-bottom: 12px;">
                        <a href="{{ path('app_quiz') }}" style="color: rgba(255, 255, 255, 0.7); text-decoration: none; font-size: 14px; transition: all 0.3s ease; display: flex; align-items: center; gap: 8px;">
                            <i class="fas fa-chevron-right" style="font-size: 10px; color: var(--neon-blue);"></i>
                            Quiz & Tests
                        </a>
                    </li>
                    <li style="margin-bottom: 12px;">
                        <a href="{{ path('fiche_index') }}" style="color: rgba(255, 255, 255, 0.7); text-decoration: none; font-size: 14px; transition: all 0.3s ease; display: flex; align-items: center; gap: 8px;">
                            <i class="fas fa-chevron-right" style="font-size: 10px; color: var(--neon-blue);"></i>
                            Fiches Collaboratives
                        </a>
                    </li>
                </ul>
            </div>
            
            <!-- Community -->
            <div>
                <h4 style="color: white; font-size: 16px; font-weight: 700; margin-bottom: 20px; display: flex; align-items: center; gap: 8px;">
                    <i class="fas fa-users" style="color: var(--neon-purple);"></i>
                    Communauté
                </h4>
                <ul style="list-style: none; padding: 0; margin: 0;">
                    <li style="margin-bottom: 12px;">
                        <a href="{{ path('groupe_index') }}" style="color: rgba(255, 255, 255, 0.7); text-decoration: none; font-size: 14px; transition: all 0.3s ease; display: flex; align-items: center; gap: 8px;">
                            <i class="fas fa-chevron-right" style="font-size: 10px; color: var(--neon-purple);"></i>
                            Groupes d'Étude
                        </a>
                    </li>
                    <li style="margin-bottom: 12px;">
                        <a href="{{ path('chat_index') }}" style="color: rgba(255, 255, 255, 0.7); text-decoration: none; font-size: 14px; transition: all 0.3s ease; display: flex; align-items: center; gap: 8px;">
                            <i class="fas fa-chevron-right" style="font-size: 10px; color: var(--neon-purple);"></i>
                            Chat en Direct
                        </a>
                    </li>
                </ul>
            </div>
            
            <!-- Contact -->
            <div>
                <h4 style="color: white; font-size: 16px; font-weight: 700; margin-bottom: 20px; display: flex; align-items: center; gap: 8px;">
                    <i class="fas fa-envelope" style="color: var(--neon-pink);"></i>
                    Contact
                </h4>
                <div style="margin-bottom: 15px;">
                    <a href="mailto:contact@baclab.tn" style="color: rgba(255, 255, 255, 0.7); text-decoration: none; font-size: 14px; display: flex; align-items: center; gap: 8px;">
                        <i class="fas fa-envelope" style="font-size: 12px; color: var(--neon-pink);"></i>
                        contact@baclab.tn
                    </a>
                </div>
                <div style="margin-bottom: 15px;">
                    <span style="color: rgba(255, 255, 255, 0.7); font-size: 14px; display: flex; align-items: center; gap: 8px;">
                        <i class="fas fa-clock" style="font-size: 12px; color: var(--neon-pink);"></i>
                        Support 24/7
                    </span>
                </div>
            </div>
        </div>
        
        <!-- Bottom Section -->
        <div style="border-top: 1px solid rgba(255, 255, 255, 0.1); padding-top: 30px; display: flex; justify-content: space-between; align-items: center; flex-wrap: wrap; gap: 20px;">
            
            <!-- Social Links -->
            <div style="display: flex; gap: 15px;">
                <a href="#" style="width: 40px; height: 40px; background: var(--glass-bg); backdrop-filter: blur(10px); border: 1px solid var(--glass-border); border-radius: 50%; display: flex; align-items: center; justify-content: center; color: white; text-decoration: none; transition: all 0.3s ease;">
                    <i class="fab fa-facebook-f"></i>
                </a>
                <a href="#" style="width: 40px; height: 40px; background: var(--glass-bg); backdrop-filter: blur(10px); border: 1px solid var(--glass-border); border-radius: 50%; display: flex; align-items: center; justify-content: center; color: white; text-decoration: none; transition: all 0.3s ease;">
                    <i class="fab fa-instagram"></i>
                </a>
                <a href="#" style="width: 40px; height: 40px; background: var(--glass-bg); backdrop-filter: blur(10px); border: 1px solid var(--glass-border); border-radius: 50%; display: flex; align-items: center; justify-content: center; color: white; text-decoration: none; transition: all 0.3s ease;">
                    <i class="fab fa-youtube"></i>
                </a>
            </div>
            
            <!-- Copyright -->
            <div style="text-align: center; flex: 1;">
                <p style="color: rgba(255, 255, 255, 0.6); font-size: 14px; margin: 0;">
                    &copy; {{ "now"|date("Y") }} BacLab. Tous droits réservés. Fait avec <i class="fas fa-heart" style="color: var(--neon-pink);"></i> en Tunisie
                </p>
            </div>
            
            <!-- Back to Top -->
            <button onclick="window.scrollTo({top: 0, behavior: 'smooth'})" style="width: 40px; height: 40px; background: linear-gradient(135deg, var(--neon-blue), var(--neon-purple)); border: none; border-radius: 50%; color: white; cursor: pointer; transition: all 0.3s ease; display: flex; align-items: center; justify-content: center;">
                <i class="fas fa-arrow-up"></i>
            </button>
        </div>
    </div>
</footer>

{% block js %}
    {# Essential JavaScript only #}
    <script src="{{ asset('front/js/vendor/jquery-1.12.4.min.js') }}"></script>
    <script src="{{ asset('front/js/bootstrap.min.js') }}"></script>

    <script>
        // SIMPLIFIED NAVBAR CONTROLLER
        document.addEventListener('DOMContentLoaded', function () {
            // Notifications badge count
            {% if app.user %}
            fetch('{{ path('notifications_count') }}')
                .then(r => r.json())
                .then(data => {
                    const badge = document.getElementById('notification-badge');
                    if (badge && data.count > 0) {
                        badge.textContent = data.count;
                    }
                })
                .catch(() => {});
            {% endif %}
            
            // Basic navbar controller
            const header = document.querySelector('.header-area');
            const scrollProgress = document.getElementById('navbarScrollProgress');
            const menuItems = document.querySelectorAll('.main-menu nav ul li a');
            
            // Simple scroll handler
            function updateNavbar() {
                const scrollY = window.scrollY;
                const documentHeight = document.documentElement.scrollHeight - window.innerHeight;
                const scrollPercent = Math.min((scrollY / documentHeight) * 100, 100);
                
                // Update scroll progress
                if (scrollProgress) {
                    scrollProgress.style.transform = `scaleX(${scrollPercent / 100})`;
                }
                
                // Sticky behavior
                if (scrollY > 50) {
                    header.classList.add('sticky');
                } else {
                    header.classList.remove('sticky');
                }
            }
            
            // Throttled scroll handler
            let ticking = false;
            window.addEventListener('scroll', function() {
                if (!ticking) {
                    requestAnimationFrame(updateNavbar);
                    ticking = true;
                }
            });
            
            // Active menu item detection
            const currentPath = window.location.pathname;
            menuItems.forEach(item => {
                const href = item.getAttribute('href');
                if (href === currentPath || (href !== '#' && currentPath.includes(href))) {
                    item.parentElement.classList.add('active');
                }
            });
            
            // Simple dropdown behavior
            const dropdownItems = document.querySelectorAll('.main-menu nav ul li');
            dropdownItems.forEach(item => {
                const submenu = item.querySelector('.submenu');
                if (submenu) {
                    let hoverTimeout;
                    
                    item.addEventListener('mouseenter', () => {
                        clearTimeout(hoverTimeout);
                    });
                    
                    item.addEventListener('mouseleave', () => {
                        hoverTimeout = setTimeout(() => {
                            // Cleanup if needed
                        }, 100);
                    });
                }
            });
            
            // Keyboard navigation
            document.addEventListener('keydown', function(e) {
                if (e.key === 'Tab') {
                    document.body.classList.add('keyboard-navigation');
                }
            });
            
            document.addEventListener('mousedown', function() {
                document.body.classList.remove('keyboard-navigation');
            });
            
            // Smooth scroll for anchor links
            document.querySelectorAll('a[href^="#"]').forEach(anchor => {
                anchor.addEventListener('click', function (e) {
                    const href = this.getAttribute('href');
                    if (href !== '#' && document.querySelector(href)) {
                        e.preventDefault();
                        document.querySelector(href).scrollIntoView({
                            behavior: 'smooth',
                            block: 'start'
                        });
                    }
                });
            });
            
            // Simple CSS for keyboard navigation
            const style = document.createElement('style');
            style.textContent = `
                .keyboard-navigation *:focus {
                    outline: 2px solid #00d4ff !important;
                    outline-offset: 2px !important;
                }
            `;
            document.head.appendChild(style);
            
            // FOOTER FUNCTIONALITY
            
            // Newsletter Form
            const newsletterForm = document.getElementById('newsletterForm');
            const newsletterSuccess = document.getElementById('newsletterSuccess');
            
            if (newsletterForm) {
                newsletterForm.addEventListener('submit', function(e) {
                    e.preventDefault();
                    
                    const email = this.querySelector('input[type="email"]').value;
                    
                    // Simulate newsletter subscription
                    if (email) {
                        // Show success message
                        newsletterSuccess.classList.add('show');
                        
                        // Reset form
                        this.reset();
                        
                        // Hide success message after 5 seconds
                        setTimeout(() => {
                            newsletterSuccess.classList.remove('show');
                        }, 5000);
                    }
                });
            }
            
            // Back to Top Button
            const backToTopBtn = document.getElementById('backToTop');
            
            if (backToTopBtn) {
                // Show/hide button based on scroll position
                window.addEventListener('scroll', function() {
                    if (window.pageYOffset > 300) {
                        backToTopBtn.style.opacity = '1';
                        backToTopBtn.style.visibility = 'visible';
                    } else {
                        backToTopBtn.style.opacity = '0';
                        backToTopBtn.style.visibility = 'hidden';
                    }
                });
                
                // Smooth scroll to top
                backToTopBtn.addEventListener('click', function() {
                    window.scrollTo({
                        top: 0,
                        behavior: 'smooth'
                    });
                });
            }
            
            // Enhanced Social Link Interactions
            const socialLinks = document.querySelectorAll('.social-link');
            socialLinks.forEach(link => {
                link.addEventListener('mouseenter', function() {
                    this.style.transform = 'translateY(-5px) scale(1.1)';
                });
                
                link.addEventListener('mouseleave', function() {
                    this.style.transform = 'translateY(0) scale(1)';
                });
            });
            
            // Footer Animation on Scroll
            const footerElements = document.querySelectorAll('.footer-brand, .footer-links, .footer-contact');
            
            const observerOptions = {
                threshold: 0.1,
                rootMargin: '0px 0px -50px 0px'
            };
            
            const footerObserver = new IntersectionObserver((entries) => {
                entries.forEach(entry => {
                    if (entry.isIntersecting) {
                        entry.target.style.opacity = '1';
                        entry.target.style.transform = 'translateY(0)';
                    }
                });
            }, observerOptions);
            
            footerElements.forEach(element => {
                element.style.opacity = '0';
                element.style.transform = 'translateY(30px)';
                element.style.transition = 'all 0.6s ease';
                footerObserver.observe(element);
            });
            
            // Newsletter Card Parallax Effect
            const newsletterCard = document.querySelector('.newsletter-card');
            if (newsletterCard) {
                window.addEventListener('scroll', function() {
                    const scrolled = window.pageYOffset;
                    const rate = scrolled * -0.1;
                    newsletterCard.style.transform = `translateY(${rate}px)`;
                });
            }
        });
    </script>
    
    {# Floating Chat Component #}
    {{ include('components/floating_chat.html.twig') }}
    
    {# Floating Chat JavaScript #}
    <script src="{{ asset('front/js/chat-floating.js') }}"></script>
{% endblock %}
</body>
</html>
