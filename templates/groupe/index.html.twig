{% extends 'base.html.twig' %}

{% block body %}
<div class="container mt-5">
    <h2>➕ Créer un groupe</h2>
    <p>Formulaire de création du groupe (à faire).</p>

    <a href="{{ path('chat_index') }}" class="btn btn-secondary mt-3">
        ← Retour au chat
    </a>
</div>
{% endblock %}
